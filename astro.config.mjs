import { defineConfig } from "astro/config";

export default defineConfig({
  output: "static",
  site: "https://ashoilev.github.io",
  base: "/yana-lozeva-photography",
  trailingSlash: "always"
});
